services:
  adminer:
    image: adminer
    container_name: adminer
    restart: unless-stopped
    ports:
      - 8081:8080
    environment:
   #  ADMINER_DESIGN: adminer-dark #dracula
      ADMINER_DEFAULT_SERVER: mysql
    networks:
      - default
    # - custom-network


networks:
  default:
    driver: bridge
# custom-network:
#   external: true
